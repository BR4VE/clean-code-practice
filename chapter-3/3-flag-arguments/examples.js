// Example 1:
// Passing a boolean into a function is a bad practice

render(true);

// BAD: It proclaims that this function does more than one thing

renderForSuite();
renderForSingleTest();

//------------------------------------------------------------------------
